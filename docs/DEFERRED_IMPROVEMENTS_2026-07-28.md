# 保留事項（現行構成）

更新日: 2026-08-08

大規模な機能追加は行わず、今回の再構成で必要な導線・分類・URL・検証を優先した。次回以降に検討する項目は次のとおり。

- 学習履歴を端末間で同期する仕組み（現状はブラウザ内保存）。
- 動画・講義ノートの検索性を、既存のジャンル・目次を壊さず補助するUI。
- 静的な問題検索カードの初期転送量を減らす方式（JavaScript無効時の可読性とのトレードオフを再検討）。
- アクセス解析の学習導線別レポート。

これらは現行の正規URL、タグAND検索、動画21ジャンル、講義ノートの機能を変更せずに実装できる見通しが立つまで保留する。
